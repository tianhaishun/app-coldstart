# App 冷启测速 —— 使用说明

用这份说明学会：**怎么测安卓 App 冷启动要多久**。不需要会写代码。

---

## 它是干什么的？

测两类启动时间：

- **首次冷启动**：卸装重装后，第一次打开 App（最干净，常会弹权限窗）
- **二次冷启动**：不卸装，只关掉进程再打开

工具可以自动跑多轮：卸装 → 测首次 → 测二次 → 再卸装……最后算出平均耗时（会去掉极端大/小值）。

需要：安卓测试机、USB 连电脑、打开 **USB 调试**。

---

## 怎么打开？

1. 手机连电脑，授权 USB 调试  
2. 双击 **`Start.bat`**（第一次可能要等几分钟装环境）  
3. 不要关弹出的黑色窗口  
4. 浏览器打开后，在顶部选中你的手机  

同事用同一 Wi‑Fi 访问时，用启动窗口里打印的 **电脑 IP 地址** 打开即可（不要用看起来像 `0.0.0.0` 的地址）。

---

## 页面三块分别干什么？

| 位置 | 作用 |
|------|------|
| **左边** | 看手机画面；设模板时在这里点选 |
| **中间** | 自动测速：设模板、开始/停止、看运行日志 |
| **右边** | 填包名、上传安装包、看历史记录和统计报告 |

---

## 第一次怎么用？（按顺序做）

### 1）（可选）建一个项目

顶部「项目」：

- **新建** → 起个名字（如「某某 GP」）  
- 配好模板后点 **保存**  
- 下次直接选这个项目加载（**安装包不会被保存**，每次仍要自己上传）

### 2）填包名、上传安装包

右边：

- 填好 **应用包名**  
- **上传 APK**（测首次冷启动时会用来卸装重装）

启动方式建议选 **用包名启动**（更稳）。

### 3）设「启动成功」样子（必做）

用来告诉工具：**怎样算启动完成、可以停表**。

1. 先手动把 App 打开到你认为「已经启动成功」的画面  
2. 中间点 **设启动成功模板**  
3. 在左边画面上点一个 **固定、好认** 的地方（按钮、图标、标题等）  
4. **不要点大片空白纯色**（容易设失败或测不准）

### 4）设「跳过弹窗」按钮（建议做）

首次安装常会弹「是否允许通知」等。

1. 中间点 **添加跳过**  
2. 点弹窗上的「允许 / 不允许」等按钮  
3. 自动跑的时候：  
   - **只有首次冷启动**会自动点这些  
   - 点掉不算停表  
   - **二次冷启动不会再去点**（一般也不会再弹）

### 5）参数

一般用默认即可：

- **轮数**：跑几轮（一轮 = 卸装 + 首次 + 二次）  
- **异常阈值**：太久才成功的样本会标成异常，但不会整轮直接放弃  
- 其它间隔/冷却：无特殊需求保持默认

### 6）开始

1. 看一眼「开跑检查」是否都就绪  
2. 点 **开始自动循环**  
3. 盯着中间的 **运行日志** 和右边的记录  
4. 要停就点 **停止**  
5. 全部跑完后看 **报告** 和统计

跑的过程中左侧直播可能会暂时关掉，属于正常现象。

---

## 自动跑一轮时，你会看到什么？

大致顺序：

1. 卸装并重装  
2. 测 **首次**：清进程 → 启动 →（可能自动点权限）→ 等到「启动成功」画面 → 记下秒数  
3. 测 **二次**：再清进程 → 再启动 → 等到成功画面 → 再记一条  
4. 进入下一轮  

历史里的单数条一般是首次、双数条一般是二次。  
**尽量不要删中间某条记录**，否则后面「首次/二次」会对不齐。

---

## 时间是怎么来的？（只需知道这些）

- **开始计时**：电脑发出「启动」并得到回应之后  
- **结束计时**：屏幕上再次出现你设的「启动成功」样子  
- 记入的秒数会扣掉「最后一次为了认画面而去截屏」所花的时间（传图本身不算进冷启动）  
- 中间若弹出权限窗，工具会先帮你点掉，**点权限不算结束**  
- 超过异常阈值的样本会标出来，方便你之后判断要不要作废/补测  

统计时：首次、二次分开算平均；样本够多时会去掉一个最大和一个最小。  
若平台选成 iOS，首次结果还会再减 1 秒（扣测试弹窗一类时间）；安卓（GP）不减。

---

## 日志大概怎么看？

- 正在卸装 / 杀进程 / 启动 → 进度说明  
- 点了跳过弹窗 → 只是点掉，还在计时  
- 等待中 → 还在认「启动成功」画面  
- 匹配成功 / 写入记录 → 这一次测完了  

看不懂细节时，以 **是否写出一条耗时记录**、右边统计是否更新 为准即可。

---

## 常见问题

**重启后模板没了？**  
用顶部项目重新加载，或重新设模板并保存。

**开跑检查不过？**  
确认已选手机、已设启动成功模板、已上传 APK、包名正确；页面可强制刷新后再加载项目。

**二次测得很久、一直「等待」？**  
先确认启动成功模板是不是点得太「泛」（桌面也可能很像）。换一个首页上更独特的元素重设，保存后再测。

**卸装很慢？**  
安装包大时正常，等日志里出现卸装/安装成功即可。

**别人打不开我的页面？**  
用启动窗口里的 IP 地址；确认同一网络；公司电脑可能要放行防火墙。

---

## 注意

请只在 **测试机** 上使用。选错包名或安装包可能卸掉/覆盖你不该动的应用。  
局域网共享时，同网段的人也能操作这台测试机，请只在可信环境使用。
