# POC-only dependencies. Do not add to the production backend until the
# frame-stream POC meets the FPS/latency/memory acceptance criteria.
adbutils==2.12.0
av==18.0.0
numpy>=1.24,<3.0
opencv-python>=4.8,<6.0
