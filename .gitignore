# Python 运行产物
__pycache__/
*.pyc

# 虚拟环境
.venv/

# Claude Code 本地配置
.claude/

# ZCode agent 本地配置 + plan 文件（agent 工具自动生成，与项目无关）
.zcode/

# 临时文件（防御性，正常不会在项目目录）
_cst_*.png
_cst_*.apk

# 用户冷启动项目配置（含模板图，本机私有，不入版本库）
projects/

# Stitch 生成的 UI 设计稿（仅作设计参考，非项目运行所需，不入版本控制）
stitch_adaptive_responsive_shortcut_layout/
chk.html

# 本地 npm 依赖（若有前端工具链，不入版本库）
node_modules/
