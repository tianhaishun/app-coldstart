# 冷启动计时器 v2 —— Python 依赖
# 安装：pip install -r requirements.txt
# 首次双击 Start.bat 会自动建 venv 并装这些
#
# 版本上界说明：加 <X.0 防止大版本破坏性更新（如 numpy 2.0 的 C API 变更）。
# 当前已验证组合：fastapi 0.139 / numpy 2.4 / opencv 5.0 / rapidocr 1.4。
# 升版本时需重新验证 OCR + matchTemplate + raw screencap 解析。

fastapi>=0.110,<1.0
# FastAPI 处理 UploadFile/表单数据必须的依赖（/api/upload_apk 用到）。
# FastAPI 官方规定：凡有 File()/UploadFile/Form() 都需显式声明，否则装饰器阶段就抛 RuntimeError。
python-multipart>=0.0.7,<1.0
uvicorn[standard]>=0.27,<1.0
rapidocr-onnxruntime>=1.3.24,<2.0
opencv-python>=4.8,<6.0
numpy>=1.24,<3.0
# scrcpy 视频流解码（可选视频通道；不可用时自动回退 ADB 截图）
av>=18.0,<19.0
# iOS 设备通信（截图/设备检测/App 管理）。pymobiledevice3 10.x 全异步 API。
# 需要 Windows 端 Apple Mobile Device Service（AMDS）作为 usbmuxd 后端。
pymobiledevice3>=10.0,<11.0
